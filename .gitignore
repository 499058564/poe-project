# Gradle
.gradle/
build/

# Java / JVM
out/
*.class
hs_err_pid*
replay_pid*

# IntelliJ IDEA
.idea/
*.iml

# Editors
.vscode/

# OS files
.DS_Store
Thumbs.db

# ========== Submodules ==========

# PoeCharm2 — Chinese translation data submodule
# Managed via .gitmodules (ignore = untracked). Tracked as a gitlink only.
# Build artifacts inside the submodule are governed by .gitmodules settings
# and the submodule's own .gitignore.
