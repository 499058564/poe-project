# Gradle
.gradle/
build/

# Java / JVM
out/
*.class
hs_err_pid*
replay_pid*

# IntelliJ IDEA
.idea/
*.iml

# Editors
.vscode/

# OS files
.DS_Store
Thumbs.db

# ========== Submodules ==========

# PoeCharm2 — Chinese translation data submodule
# Managed via .gitmodules (ignore = dirty). Tracked as a gitlink only.
# Submodule is read-only from the parent project's perspective;
# build artifacts are ignored by the submodule's own .gitignore.