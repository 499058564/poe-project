# T-017 词缀与工艺数据同步

**版本**：v0.2.0  
**模块**：`data-provider` + `data-cache`  
**预估**：1.5 天  
**前置**：T-013  
**状态**：待开始

---

## 任务描述

同步词缀详情、工艺配方、交易经济数据。`mods` 主表已同步（T-013），本次补充子表。这些表行数大（5 万+），需特别关注分批同步效率。

## 涉及的 Wiki Cargo 表

### 词缀子表（大表，分批同步）

| 表名 | 行数 | 字段 |
|------|------|------|
| `mod_stats` | 56,774 | mod_id, stat_id, min_value, max_value |
| `mod_spawn_weights` | 49,046 | mod_id, tag, weight |
| `mod_generation_weights` | 3,773 | mod_id, generation_type, weight |
| `mod_sell_prices` | 24,463 | mod_id, amount, currency_type |

### 物品-词缀关联

| 表名 | 行数 | 字段 |
|------|------|------|
| `item_mods` | 10,590 | item_id, mod_id |
| `item_stats` | 12,003 | item_id, stat_id, value (unique item fixed stats) |
| `item_buffs` | 61 | item_id, buff_stat, buff_value |

### 工艺/配方

| 表名 | 行数 | 字段 |
|------|------|------|
| `crafting_bench_options` | 838 | name, required_level, crafting_cost, master_level |
| `crafting_bench_options_costs` | 812 | option_id, currency_type, amount |
| `essences` | 106 | name, essence_type, tier |
| `fossils` | 25 | name, fossil_type, effect_description |
| `fossil_weights` | 37 | fossil_id, mod_tag, weight_modifier |

### 经济数据

| 表名 | 行数 | 字段 |
|------|------|------|
| `vendor_rewards` | 1,303 | recipe_name, input_items, output_item, class_restriction |
| `item_sell_prices` | 2,602 | item_id, rarity, amount, currency_type |
| `item_purchase_costs` | 930 | item_id, vendor, amount, currency_type |

### ⚠️ 字段验证（实现前必须执行）

在编写 Converter 前，需逐表验证 Cargo 字段实际名称与任务文档是否一致：
1. 访问 `https://www.poewiki.net/wiki/Special:CargoTables/<table_name>` 查看完整字段列表
2. 或通过 API 测试：`cargoquery?tables=<table_name>&fields=_pageName&limit=1` 确认表可查询
3. 记录实际字段名，对比上表中列出的字段，修正不一致之处
4. 确认字段完全对齐后，再开始编写 Converter 和数据库迁移脚本

> 历史教训：`mods` 表的 mod_group 实际是 mod_groups（复数），spawn_tags/spawn_weights 实际不存在。

## 详细步骤

### 1. 数据转换器

```
ModStatConverter, ModSpawnWeightConverter, ModGenerationWeightConverter,
ModSellPriceConverter, ItemModConverter, ItemStatConverter, ItemBuffConverter,
CraftingBenchOptionConverter, EssenceConverter, FossilConverter,
VendorRewardConverter, ItemSellPriceConverter, ItemPurchaseCostConverter
```

### 2. 模型类

在 `com.poe.cache.model` 下创建对应的 14 个模型类。

### 3. DAO 层

为每个模型创建对应 DAO。

### 4. 大表分批策略

`mod_stats`（5.7 万）和 `mod_spawn_weights`（4.9 万）按 500 条/批同步，每批更新进度事件。

### 5. 数据库迁移

```sql
-- v009: 词缀子表 + 工艺 + 经济
CREATE TABLE IF NOT EXISTS mod_stats (...);
CREATE TABLE IF NOT EXISTS mod_spawn_weights (...);
-- ... 14 张表
```

## 验收标准

- [ ] 14 张表全部通过 Wiki API 完成同步
- [ ] 每张表的 Cargo 字段已通过 Special:CargoTables 页面验证，字段无缺失/错配
- [ ] `mod_stats`（5.7 万行）同步 < 5 分钟
- [ ] `mod_spawn_weights`（4.9 万行）同步 < 5 分钟
- [ ] 大表同步进度事件正确发送（每批触发）
- [ ] 各 Converter 单元测试通过
