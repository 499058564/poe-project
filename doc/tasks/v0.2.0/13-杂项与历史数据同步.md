# T-021 杂项与历史数据同步

**版本**：v0.2.0  
**模块**：`data-provider` + `data-cache`  
**预估**：1 天  
**前置**：T-013  
**状态**：待开始（低优先级）

---

## 任务描述

同步剩余的杂项表、历史数据、枚举字典、赛季特有物品。这些表非核心功能直接依赖，但提供完整性支持。

## 涉及的 Wiki Cargo 表

### 历史/版本

| 表名 | 行数 | 字段 |
|------|------|------|
| `versions` | 777 | version_name, release_date, patch_notes_url |
| `legacy_variants` | 1,994 | item_name, original_stats, nerfed_version, current_version |

### 游戏系统杂项

| 表名 | 行数 | 字段 |
|------|------|------|
| `prophecies` | 250 | prophecy_name, prophecy_text, reward, seal_cost |
| `quest_rewards` | 526 | quest_name, act, class, reward_item, reward_quantity |
| `spawn_weights` | 1,203 | item_type, area_tag, weight |
| `generic_stats` | 136 | stat_id, display_name, stat_category |
| `armour_types` | ~5 | type_name (AR/EV/ES/AR+EV/AR+ES/EV+ES/AR+EV+ES) |

### 赛季特有物品（低优先级）

| 表名 | 行数 | 赛季 |
|------|------|------|
| `tattoos` | 132 | 先祖 (Ancestor) |
| `tinctures` | 15 | 无情试炼 (Ruthless) |
| `sentinels` | 43 | 哨兵 (Sentinel) |
| `idols` | 39 | 亡灵 (Necropolis) |
| `grafts` | 16 | 移植 (Ruthless) |
| `corpse_items` | 102 | 尸爆 (Ruthless) |

### 低优先级杂项

| 表名 | 行数 | 字段 |
|------|------|------|
| `cosmetic_items` | 4,195 | mtx_name, mtx_type, shop_price |
| `hideout_doodads` | 1,387 | doodad_name, doodad_type, favour_cost |
| `guides` | 45 | guide_title, guide_author, guide_category |

### ⚠️ 字段验证（实现前必须执行）

在编写 Converter 前，需逐表验证 Cargo 字段实际名称与任务文档是否一致：
1. 访问 `https://www.poewiki.net/wiki/Special:CargoTables/<table_name>` 查看完整字段列表
2. 或通过 API 测试：`cargoquery?tables=<table_name>&fields=_pageName&limit=1` 确认表可查询
3. 记录实际字段名，对比上表中列出的字段，修正不一致之处
4. 确认字段完全对齐后，再开始编写 Converter 和数据库迁移脚本

> 历史教训：低优先级杂项表的字段结构同样可能与文档不符，所有表一视同仁进行验证。

## 详细步骤

### 1. 数据转换器

```
VersionConverter, LegacyVariantConverter, ProphecyConverter,
QuestRewardConverter, SpawnWeightConverter, GenericStatConverter,
ArmourTypeConverter, TattooConverter, TinctureConverter, SentinelConverter,
IdolConverter, GraftConverter, CorpseItemConverter, CosmeticItemConverter,
HideoutDoodadConverter, GuideConverter
```

### 2. 模型类 + DAO

16 个模型类 + 16 个 DAO。

### 3. 数据库迁移

```sql
-- v012: 杂项数据表
CREATE TABLE IF NOT EXISTS versions (...);
-- ... 16 张表
```

### 4. 同步优先级

- **第一优先**: versions, legacy_variants, generic_stats（游戏系统核心）
- **第二优先**: spawn_weights, armour_types, quest_rewards
- **第三优先**: 赛季特有物品（低使用频率）
- **最后**: cosmetic_items, hideout_doodads（非游戏数据）

## 验收标准

- [ ] 16 张表全部完成同步
- [ ] 每张表的 Cargo 字段已通过 Special:CargoTables 页面验证，字段无缺失/错配
- [ ] `versions` 表包含 777 条版本历史
- [ ] `legacy_variants` 支持查询物品历史版本
- [ ] 同步事件正确发送（低优先级表可合并为单事件）
- [ ] 各 DAO + Converter 单元测试通过
