# T-011 实现数据库迁移机制

**版本**：v0.2.0  
**模块**：`data-cache`  
**预估**：0.25 天  
**前置**：T-010  
**状态**：待开始

---

## 任务描述

实现版本化数据库迁移机制，支持增量 SQL 脚本执行和幂等性保证。

## 详细步骤

### 1. MigrationManager

```java
public class MigrationManager {
    private static final String VERSION_TABLE_SQL = """
        CREATE TABLE IF NOT EXISTS schema_version (
            version     INTEGER PRIMARY KEY,
            description TEXT,
            executed_at TEXT DEFAULT (datetime('now'))
        );
    """;

    /**
     * 执行所有未执行的迁移脚本
     * 脚本命名规则: migration/v{版本号}_{描述}.sql
     * 例如: migration/v001_init.sql, migration/v002_add_index.sql
     */
    public void migrate() {
        ensureVersionTable();
        int currentVersion = getCurrentVersion();
        
        List<String> pendingMigrations = findPendingMigrations(currentVersion);
        for (String migration : pendingMigrations) {
            executeMigration(migration);
        }
    }

    private void executeMigration(String sqlFile) {
        // 1. 读取 SQL 文件
        // 2. 在事务中执行
        // 3. 更新 schema_version 表
        // 4. 失败时回滚
    }
}
```

### 2. 迁移脚本管理

```
data-cache/src/main/resources/migration/
├── v001_init.sql       # 初始表结构
├── v002_add_fts.sql    # 全文搜索索引
└── v003_add_maps.sql   # 地图数据表
```

## 验收标准

- [ ] 首次运行执行所有迁移
- [ ] 二次运行不重复执行
- [ ] 新增迁移脚本自动执行
- [ ] 迁移失败不破坏已有数据
