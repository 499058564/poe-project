# T-018 技能与天赋详情同步

**版本**：v0.2.0  
**模块**：`data-provider` + `data-cache`  
**预估**：1.5 天  
**前置**：T-013  
**状态**：待开始

---

## 任务描述

同步技能和天赋树的详细数据。所有表 Wiki Cargo API 已验证可正常查询。`skill_gems` 之前误报 MWException 是因为用了不存在的字段名 `name`，实际字段是 `skill_id`。

## 涉及的 Wiki Cargo 表

### 技能详细数据（大表）

| 表名 | 行数 | 字段 | API 状态 |
|------|------|------|----------|
| `skill` | 12,390 | skill_id, skill_type, cost_type, is_mana_reservation | ⚠️ 待验证 |
| `skill_levels` | 84,322 | skill_id, level, experience, mana_cost, damage_multiplier | ⚠️ 待验证 |
| `skill_stats_per_level` | 190,769 | skill_id, level, stat_id, value | ⚠️ 待验证 |
| `skill_quality` | 834 | skill_id, quality_stat, quality_value | ⚠️ 待验证 |
| `skill_quality_stats` | 847 | quality_id, stat_id, value_per_quality | ⚠️ 待验证 |
| `gem_levels` | 30,671 | gem_id, level, required_level, str/dex/int, experience | ⚠️ 待验证 |
| `skill_gems` | 815 | skill_id, gem_tags, primary_attribute, max_level, is_vaal_skill_gem, support_gem_letter, requires_*, *_variant_id 等 16 字段 | ✅ 正常（字段名是 `skill_id`，不是 `name`）|
| `alternate_quality` | ~900 | gem_id, quality_type, quality_stat | ⚠️ 待验证 |
| `active_skill_types` | ~100 | type_id, type_name (spell/attack/aoe/projectile) | ⚠️ 待验证 |

### 天赋详细数据

| 表名 | 行数 | 字段 | API 状态 |
|------|------|------|----------|
| `passive_skills` | 5,883 | name, is_keystone, is_notable, stat_text | ✅ 正常（含 Acrobatics 等）|
| `passive_skill_connections` | 6,717 | from_node, to_node | ⚠️ 待验证 |
| `mastery_effects` | 353 | group_id, effect_stat, effect_value | ⚠️ 待验证 |
| `mastery_groups` | 61 | group_name, associated_cluster | ⚠️ 待验证 |
| `alternate_tree_versions` | ~30 | jewel_name, affected_nodes, modified_stats | ⚠️ 待验证 |

### 职业数据

| 表名 | 行数 | 字段 |
|------|------|------|
| `character_classes` | 7 | name, base_str/dex/int, ascendancy_count |
| `ascendancy_classes` | ~50 | name, base_class, notable_count |

### ⚠️ 字段验证（实现前必须执行）

在编写 Converter 前，需逐表验证 Cargo 字段实际名称与任务文档是否一致：
1. 访问 `https://www.poewiki.net/wiki/Special:CargoTables/<table_name>` 查看完整字段列表
2. 或通过 API 测试：`cargoquery?tables=<table_name>&fields=_pageName&limit=1` 确认表可查询
3. 记录实际字段名，对比上表中列出的字段，修正不一致之处
4. 确认字段完全对齐后，再开始编写 Converter 和数据库迁移脚本

> 历史教训：`skill_gems` 用 `name` 字段查询返回 MWException，实际字段是 `skill_id`。
> API 返回 MWException 通常意味着字段名写错，而非表本身不可用。

## 详细步骤

### 1. API 可用性验证

先对每个表执行一次 `cargoquery` 测试调用：
```java
// 返回数据 → 照常同步
// 注意: 返回 MWException 通常是字段名不匹配（如用 name 而非 skill_id），而非表本身不可用
```

### 2. 技能表策略

所有表均从 Wiki Cargo API 同步。`skill_gems` 字段名为 `skill_id`（非 `name`），注意查询时使用正确字段。大表（`skill_stats_per_level` 等）分批 500 条同步。

### 3. 天赋表策略

全部从 Wiki Cargo API 获取（已验证 `passive_skills` 可正常返回 5,883 条）。

### 4. 技能大表分批

`skill_stats_per_level`（19 万行）和 `skill_levels`（8.4 万行）按 500 条/批同步，分期处理避免阻塞。

### 5. 数据转换器

```
SkillConverter, SkillLevelConverter, SkillStatsPerLevelConverter,
SkillQualityConverter, SkillQualityStatsConverter, GemLevelConverter,
PassiveSkillConnectionConverter, MasteryEffectConverter,
AscendancyConverter, CharacterClassConverter
```

## 验收标准

- [ ] 每个表的 Cargo 字段已通过 Special:CargoTables 页面验证，字段无缺失/错配
- [ ] `passive_skills`（5,883 条）正常同步
- [ ] `skill_gems`（815 条）使用正确字段名 `skill_id` 同步
- [ ] `skill_stats_per_level` 同步 < 15 分钟（19 万行）
- [ ] `passive_skill_connections` 数据正确，可从任意节点查邻居
