# T-019 怪物与世界数据同步

**版本**：v0.2.0  
**模块**：`data-provider` + `data-cache`  
**预估**：1 天  
**前置**：T-013  
**状态**：待开始

---

## 任务描述

同步怪物、游戏区域、异界图鉴数据。这些表为伤害模拟、区域信息查询提供基础数据。

## 涉及的 Wiki Cargo 表

### 怪物

| 表名 | 行数 | 字段 |
|------|------|------|
| `monsters` | 10,642 | name, life, damage, attack_speed, resistances, monster_type, monster_tags |
| `monster_types` | 3,534 | type_name, description |
| `monster_base_stats` | 100 | level, life, damage, armour, evasion, accuracy, monster_type |
| `monster_life_scaling` | 100 | level, life_multiplier |
| `monster_map_multipliers` | 82 | monster_type, map_tier, damage_mult, life_mult |
| `monster_resistances` | 50 | monster_type, fire/cold/lightning/chaos_resist |

### 区域

| 表名 | 行数 | 字段 |
|------|------|------|
| `areas` | ~1,500 | area_name, act, monster_level, boss_name, waypoint, connections |
| `area_connections` | ~200 | from_area, to_area, direction |

### 异界图鉴

| 表名 | 行数 | 字段 |
|------|------|------|
| `atlas_maps` | ~150 | map_id, atlas_x, atlas_y, tier, region |
| `atlas_nodes` | ~600 | node_id, node_type, connected_nodes, position |
| `atlas_passives` | ~500 | node_name, passive_effect, required_maps |
| `atlas_passives_effects` | ~300 | passive_id, effect_stat, effect_value |
| `atlas_bosses` | ~100 | boss_name, map_name, boss_skills |
| `atlas_memory` | ~20 | memory_name, description, modifiers |
| `atlas_upgrades` | ~100 | upgrade_name, required_completion, effect |
| `atlas_upgrades_completion` | ~100 | upgrade_id, completion_condition, progress |

### ⚠️ 字段验证（实现前必须执行）

在编写 Converter 前，需逐表验证 Cargo 字段实际名称与任务文档是否一致：
1. 访问 `https://www.poewiki.net/wiki/Special:CargoTables/<table_name>` 查看完整字段列表
2. 或通过 API 测试：`cargoquery?tables=<table_name>&fields=_pageName&limit=1` 确认表可查询
3. 记录实际字段名，对比上表中列出的字段，修正不一致之处
4. 确认字段完全对齐后，再开始编写 Converter 和数据库迁移脚本

> 历史教训：Wiki Cargo 字段名可能与直觉不符（单数/复数、缩写、下划线格式差异）。
> 例如 `monsters` 表的 resistance 字段可能以 `resist_` 前缀命名，需实测确认。

## 详细步骤

### 1. 数据转换器

```
MonsterConverter, MonsterTypeConverter, MonsterBaseStatsConverter,
MonsterLifeScalingConverter, MonsterMapMultiplierConverter,
MonsterResistanceConverter, AreaConverter, AreaConnectionConverter,
AtlasMapConverter, AtlasNodeConverter, AtlasPassiveConverter,
AtlasPassiveEffectConverter, AtlasBossConverter, AtlasMemoryConverter,
AtlasUpgradeConverter, AtlasUpgradeCompletionConverter
```

### 2. 模型类

16 个模型类，在 `com.poe.cache.model` 下创建。

### 3. DAO 层

16 个 DAO，在 `com.poe.cache.dao` 下创建。

### 4. 数据库迁移

```sql
-- v010: 怪物 + 世界 + 图鉴
CREATE TABLE IF NOT EXISTS monsters (...);
-- ... 16 张表
```

## 验收标准

- [ ] 16 张表全部完成同步
- [ ] 每张表的 Cargo 字段已通过 Special:CargoTables 页面验证，字段无缺失/错配
- [ ] `monsters`（1 万行）同步 < 3 分钟
- [ ] `areas` 数据包含所有 Act + 终局区域
- [ ] `atlas_nodes` + `atlas_passives` 可渲染完整图鉴
- [ ] 各 DAO 通过单元测试
