# T-016 物品搜索页面

**版本**：v0.3.0  
**模块**：`app-ui`  
**预估**：1 天  
**前置**：T-014, T-015  
**状态**：待开始

---
### 待办
1. poecharm2模块会提交东西
2. 完成单表拉取的功能,避免每次全量拉取
3. 确认拉取逻辑
4. items在wiki上有数据,但拉取不下来




## ⚠️ 已知阻塞问题：Wiki 数据同步失败表

> 以下问题记录自 seed.db 最近一次重建（`buildSeedDb`，耗时约 50 分钟）。
> 共 91 张 Wiki Cargo 表，当前 89 张表有数据，2 张完全空表，2 张部分失败。
> 由于数据同步策略为「仅通过 seed.db 复制，不自动同步」，这些表在 seed.db 重建成功前均不可用。

### 完全空表（阻塞物品搜索功能）

| # | Wiki Cargo 表 | 本地表 | 行数 | 失败原因 | 影响 |
|---|-------------|--------|------|---------|------|
| 1 | `items` | `base_items` | 0 | Wiki 服务端 `MWException`：Cargo 扩展在查询 78 字段时崩溃。即使缩小 batchSize=100 也无法绕过。这是 PoE Wiki 基础设施问题，客户端不可修复。 | **物品搜索全部不可用**，`items_fts` 也因此为空 |
| 2 | `items_fts` | `items_fts` | 0 | 依赖 `base_items` 有数据后重建 FTS 索引 | FTS5 全文搜索不可用 |

### 部分失败表

| # | Wiki Cargo 表 | 本地表 | 实际/预期行数 | 失败原因 |
|---|-------------|--------|-------------|---------|
| 3 | `mods` | `mods` | 10,000 / 39,294 | Cloudflare 持续封锁中断同步，仅完成约 25%。词缀搜索可能缺数据。 |
| 4 | `skill_gems` | `skill_gems` | 815 (数据完整) | 数据量正常，但 `require_intelligence` / `require_dexterity` / `require_strength` 字段存储为 `boolean` 而非 `integer`，待修复类型映射。 |

### 已修复的历史失败表（参考）

| Wiki Cargo 表 | 曾失败原因 | 修复方案 |
|-------------|---------|---------|
| `areas` | Cloudflare 429 + JSON 截断（1MB+） | batchSize=200 + Cloudflare 检测 + JSON 截断重试 → 当前 1,884 rows ✅ |
| `passive_skills` | 缺失 6 个字段 | v016 migration 补齐 → 当前 5,883 rows ✅ |

### 解决方案（待定）

- **items 表**：Wiki Cargo 源不可用，需寻找替代数据源（poe.ninja API / RePoE / 本地 POB 数据提取）
- **mods 表部分失败**：增加重试次数或分时段多次运行 `buildSeedDb` 累积补齐
- **skill_gems 类型错误**：在 converter 中修正 boolean → integer 映射

---

## 任务描述

实现物品搜索 UI 页面：搜索栏 + 结果列表 + 详情面板。

## 详细步骤

### 1. 页面布局

```
┌────────────────────────────────────────────────┐
│  [搜索框: ______________] [类别▼] [搜索]       │
├────────────┬───────────────────────────────────┤
│  结果列表   │  详情面板                         │
│            │  ┌─────────────────────────┐      │
│  Mageblood │  │ Mageblood              │      │
│  传奇 重革腰带│  │ 传奇 重革腰带             │      │
│            │  │ ─────────────────      │      │
│  Headhunter│  │ 需求:                    │      │
│  传奇 皮革腰带│  │  等级: 44              │      │
│            │  │                         │      │
│  ...       │  │ 固定词缀:                │      │
│            │  │  +15-25 全属性            │      │
│            │  │                         │      │
│            │  │ 传奇词缀:                │      │
│            │  │  药水效果持续期间...       │      │
│            │  └─────────────────────────┘      │
├────────────┴───────────────────────────────────┤
│  共 5120 条结果  第 1/103 页  [上一页] [下一页] │
└────────────────────────────────────────────────┘
```

### 2. 搜索交互

- [ ] 回车键触发搜索
- [ ] 输入防抖 300ms（输入停止 300ms 后自动搜索）
- [ ] 搜索中显示 loading 动画（ProgressIndicator）
- [ ] 空结果友好提示："未找到匹配的物品"
- [ ] 错误处理：网络/数据库异常提示

### 3. 结果列表

- [ ] 每行显示：物品名、中文名、类别、等级需求
- [ ] 按稀有度着色：
  - 普通 `#c8c8c8`、魔法 `#8888ff`、稀有 `#ffff77`、传奇 `#af6025`
- [ ] 点击列表项 → 右侧显示详情 + 异步加载完整数据

### 4. 详情面板

- [ ] 物品名称（带稀有度颜色）
- [ ] 物品类别 + 基底类型
- [ ] 需求属性（等级、力量、敏捷、智慧）
- [ ] 固定词缀列表
- [ ] 传奇专属词缀
- [ ] 背景描述文字（斜体）

## 验收标准

- [ ] 搜索 "Mageblood" 显示正确结果
- [ ] 搜索 "法师之血" 同样返回 Mageblood
- [ ] 点击结果查看详情
- [ ] 分页功能正常
- [ ] 空搜索词时不崩溃
