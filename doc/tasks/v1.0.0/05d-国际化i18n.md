# T-036d 国际化(i18n)

**版本**：v1.0.0  
**模块**：`app-ui`  
**预估**：0.5 天  
**前置**：所有功能完成  
**状态**：待开始

---

## 任务描述

实现应用界面的多语言支持，支持中英文切换无需重启。

## 详细步骤

### 1. 资源文件

```
app-ui/src/main/resources/i18n/
├── messages.properties           # 默认英文
├── messages_zh_CN.properties     # 简体中文
└── messages_zh_TW.properties     # 繁体中文
```

### 2. I18n 工具类

```java
public class I18n {
    private static ResourceBundle bundle;

    public static void setLocale(Locale locale) {
        bundle = ResourceBundle.getBundle("i18n/messages", locale);
        // 发送语言变更事件
        AppEventBus.postSync(new LocaleChangedEvent(locale));
    }

    public static String get(String key, Object... args) {
        return MessageFormat.format(bundle.getString(key), args);
    }
}
```

### 3. 语言切换

```java
// 设置页面选择语言
// 发送 LocaleChangedEvent
// 所有注册的 UI 组件重新加载文本
// 无需重启应用
```

### 4. 翻译覆盖范围

- [ ] 菜单栏
- [ ] 导航栏
- [ ] 设置页面
- [ ] 状态栏
- [ ] 错误提示
- [ ] 确认对话框
- [ ] 各功能页面标题/按钮/标签

### 5. 翻译文件示例

```properties
# messages_zh_CN.properties
app.title=POE 工具
nav.items=物品搜索
nav.gems=技能宝石
nav.tree=天赋树
nav.craft=装备模拟
nav.settings=设置
search.placeholder=搜索物品...
search.button=搜索
common.loading=加载中...
common.error=出错了
common.retry=重试
```

## 验收标准

- [ ] 中英文切换正常
- [ ] 切换无需重启
- [ ] 所有 UI 文本覆盖
- [ ] 翻译键缺失时 fallback 到英文
