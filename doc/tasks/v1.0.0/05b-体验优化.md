# T-036b 体验优化

**版本**：v1.0.0  
**模块**：`app-ui`  
**预估**：0.5 天  
**前置**：所有功能完成  
**状态**：待开始

---

## 任务描述

提升用户体验：快捷键、自动更新、崩溃恢复、新手引导。

## 详细步骤

### 1. 快捷键系统

```java
public class ShortcutManager {
    public void register() {
        // Ctrl+F     → 聚焦全局搜索框
        // Ctrl+Tab   → 切换下一个 Tab
        // Ctrl+1~9   → 切换到指定 Tab
        // Ctrl+N     → 新建 BD（如果有）
        // F5         → 刷新当前页面数据
        // Escape     → 关闭当前弹窗/Tooltip
    }
}
```

### 2. 自动更新检查

```java
public class UpdateChecker {
    /** 启动时异步检查 GitHub Release */
    public void checkForUpdates() {
        // GET https://api.github.com/repos/{owner}/{repo}/releases/latest
        // 比较 tag_name 与当前版本
        // 有新版本 → 通知栏提示（非阻塞弹窗）
    }
}
```

### 3. 崩溃恢复

- [ ] 全局未捕获异常 → 记录日志 + 显示友好提示
- [ ] 未保存的 BD/编辑 → 自动保存到临时目录
- [ ] 下次启动 → 检测临时保存 → 提示恢复
- [ ] 崩溃日志上传（可选，用户同意后）

### 4. 新手引导

- [ ] 首次启动 → 显示功能导览（3-5 步）
- [ ] 每个主要页面 → 右上角 Help 按钮 → 显示使用提示
- [ ] 空状态引导（无数据时显示操作指引，而非空白页）

## 验收标准

- [ ] 快捷键全部生效
- [ ] 自动更新提示不阻塞启动
- [ ] 崩溃后数据可恢复
- [ ] 首次启动引导友好
