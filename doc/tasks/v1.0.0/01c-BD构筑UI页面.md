# T-032c BD 构筑 UI 页面

**版本**：v1.0.0  
**模块**：`app-ui`  
**预估**：1 天  
**前置**：T-032b  
**状态**：待开始

---

## 任务描述

实现 BD 构筑的前端 UI：偏好输入表单 + 流式生成展示 + BD 详情查看。

## 详细步骤

### 1. 页面布局

```
┌────────────────────────────────────────────────┐
│  AI BD 构筑                                     │
├──────────────────────┬─────────────────────────┤
│  偏好设置             │  生成结果                │
│                      │                         │
│  升华职业:            │  正在生成 BD 方案...     │
│  [全部 ▼]            │                         │
│                      │  ████████████░░ 60%     │
│  主技能:              │                         │
│  [任意 ▼]            │  流式输出区域:            │
│                      │  根据你的偏好，我推荐     │
│  玩法风格:            │  龙卷射击锐眼...         │
│  ○ 近战 ○ 远程       │                         │
│  ○ 法术 ○ 召唤       │                         │
│                      │                         │
│  预算:                │                         │
│  ○ 开荒 ○ 中期       │                         │
│  ○ 后期 ○ 无限制     │                         │
│                      │                         │
│  □ 专家模式           │                         │
│                      │                         │
│  额外说明:            │                         │
│  ┌─────────────────┐ │                         │
│  │                 │ │                         │
│  └─────────────────┘ │                         │
│                      │                         │
│  [▶ 生成 BD 方案]    │                         │
└────────────────────────────────────────────────┘
```

### 2. 偏好表单

- [ ] 升华职业下拉框（从游戏数据加载）
- [ ] 主技能搜索框（支持模糊搜索）
- [ ] 玩法风格单选组（图标化展示）
- [ ] 预算档位单选组
- [ ] 专家模式复选框
- [ ] 额外说明文本区

### 3. 生成结果展示

```java
// 流式输出：逐字显示 AI 回复
// 生成完成后：结构化展示
//   - 概况卡片（职业、造价、优缺点）
//   - 天赋树关键节点列表
//   - 装备推荐卡片（核心 / 替代）
//   - 技能连線图
//   - 防御机制标签
//   - 升级指南折叠面板
```

### 4. BD 详情交互

- [ ] "查看天赋树" → 跳转到天赋树页面并加载推荐节点
- [ ] "复制 POB 代码" → 生成 POB import URL
- [ ] "导出为 Markdown" → 生成 BD 分享文档
- [ ] "保存到我的 BD" → 持久化存储
- [ ] "重新生成" → 保留偏好，重新请求
- [ ] "修改偏好" → 回到编辑模式

### 5. 错误状态

- [ ] AI 服务不可用 → 提示配置
- [ ] 生成超时 → 重试按钮
- [ ] 解析失败 → 显示原始输出 + 重试

## 验收标准

- [ ] 偏好表单完整可用
- [ ] 流式生成逐字显示
- [ ] 结果结构化展示清晰
- [ ] 跳转天赋树页面正常
- [ ] 复制 POB 代码可用
- [ ] 离线/错误状态友好提示
