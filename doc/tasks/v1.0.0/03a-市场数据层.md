# T-034a 市场数据层

**版本**：v1.0.0  
**模块**：`data-provider`  
**预估**：1 天  
**前置**：T-014  
**状态**：待开始

---

## 任务描述

实现 poe.ninja 数据抓取和本地价格缓存。

## 详细步骤

### 1. NinjaApiClient

```java
public class NinjaApiClient {
    private static final String NINJA_API = "https://poe.ninja/api/data/";
    private final OkHttpClient httpClient;
    private final RateLimiter rateLimiter;  // 每秒 2 次

    /** 通货价格 */
    public List<CurrencyPrice> getCurrencyPrices(String league) {
        // GET /api/data/currencyoverview?league={league}&type=Currency
    }

    /** 物品价格（按类型） */
    public List<ItemPrice> getItemPrices(String league, ItemType type) {
        // GET /api/data/itemoverview?league={league}&type={type}
    }

    /** 批量物品价格 */
    public List<ItemPrice> getBulkPrices(String league, BulkType type) {
        // 精华、化石、油瓶、催化剂、圣甲虫等
    }
}
```

### 2. 数据模型

```java
class CurrencyPrice {
    String name;
    double chaosEquivalent;    // 1 个该通货 = ? 混沌石
    double divineEquivalent;   // 1 个该通货 = ? 神圣石
}

class ItemPrice {
    String name;
    String baseType;
    int links;                 // 连線数（6连装备溢价）
    double chaosValue;
    double divineValue;
    int listingCount;          // 在售数量（流动性参考）
}
```

### 3. PriceCache

```java
public class PriceCache {
    private static final Duration TTL = Duration.ofHours(1);
    private Map<String, CachedEntry<?>> cache = new ConcurrentHashMap<>();

    public <T> Optional<T> get(String key, Class<T> type) { ... }
    public void put(String key, Object data) { ... }
    public boolean isExpired(String key) { ... }
    public void clear() { ... }

    // 持久化到 SQLite（离线可用）
    public void persist() { ... }
    public void loadFromDisk() { ... }
}
```

### 4. 价格查询服务

```java
public class PriceService {
    private final NinjaApiClient ninjaClient;
    private final PriceCache cache;

    /** 获取物品价格（混沌石等价） */
    public double getPriceChaos(String itemName) { ... }

    /** 获取通货兑换率 */
    public double getExchangeRate(String from, String to) { ... }
}
```

## 验收标准

- [ ] 通货价格获取正确
- [ ] 物品价格查询正常
- [ ] 缓存 1 小时过期
- [ ] 离线使用缓存数据
- [ ] 网络异常时降级
